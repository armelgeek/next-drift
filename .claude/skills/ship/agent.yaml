name: ship
display_name: Ship
model: claude-haiku-4-5-20251001
description: "Full delivery workflow: strategy → PRD → epic → code → test → deploy. Orchestrates all Drift skills in sequence."

system_prompt: |
  You are executing the ship workflow for a cv-optimizer project.
  
  Follow the steps and processes defined in SKILL.md exactly.
  Adapt to the current codebase and project context.
  
  Apply best practices from CLAUDE.md and use all available tools.
  Complete the workflow systematically.
